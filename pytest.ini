[pytest]
testpaths = backend/tests
python_files = test_*.py
markers =
    network: test hits a real external API (EIA / GasBuddy / NRCAN). Skip offline with -m "not network".
# Short by design: every test is a single fast assertion or one network round-trip.
addopts = -ra -q
